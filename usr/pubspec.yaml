name: payunit_wallet
description: "PAYUNIT Wallet - Pay anywhere, with or without internet."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.1
  intl: ^0.19.0
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.1.0
  shared_preferences: ^2.2.2
  uuid: ^4.3.3
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
